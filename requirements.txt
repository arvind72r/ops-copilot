# OpsPilot — Python Dependencies
# Install: pip install -r requirements.txt

# ── Phase 2 (Baseline) ────────────────────────────────────────────────────────
pandas>=2.0.0

# ── Phase 3 (LLM Integration) — install before Phase 3 ───────────────────────
openai>=1.30.0
python-dotenv>=1.0.0

# ── Phase 4 (RAG / Retrieval) — install before Phase 4 ───────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
chromadb>=0.5.0
tiktoken>=0.7.0

# ── Phase 5+ (Tools, Memory, Deployment) ─────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
